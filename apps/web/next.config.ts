import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['avatars.githubusercontent.com', 'github.com', 'assets.aceternity.com', 'pbs.twimg.com', 'ton.x.com'],
  },
  
};

export default nextConfig;
